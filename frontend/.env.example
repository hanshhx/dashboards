# Next 서버가 /api 요청을 프록시할 백엔드 주소 (서버 사이드에서만 사용)
# 같은 서버에서 백엔드를 8080으로 띄우면 localhost, Funnel이면 그 https 주소
BACKEND_URL=http://localhost:8080

# 백엔드 API 키 (서버 사이드 전용 — 브라우저 노출 안 됨). 백엔드의 API_KEY와 동일 값.
# 비워두면 키 없이 호출. 공개 배포 시 반드시 설정.
API_KEY=
